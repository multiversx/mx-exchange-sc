name: On pull request, build contracts

on:
  pull_request:

permissions:
  contents: write

jobs:
  build:
    uses: multiversx/mx-sc-actions/.github/workflows/reproducible-build.yml@67554b32f2e7378835cb97ee7a6530f43e2eb627
    with:
      image_tag: v4.2.0
      package_whole_project_src: true
